#version 330 core
in vec3 aPos;
in vec3 aColor;

in vec3 normalSmooth;

out vec3 color;
out vec3 vertexPos;
//out vec3 normalSmoothG;

uniform vec3 positionDec;

vec3 light_color = vec3(1.0, 1.0, 1.0);
vec3 light_position = vec3(-10.0, -10.0, -50.0);

void main() {

  gl_Position = vec4(aPos + positionDec, 1.0);

  //float c = clamp(dot(normalize(aPos - light_position), normalSmooth), 0.0, 1.0);
  color = aColor;
  //normalSmoothG = normalSmooth;
  vertexPos = aPos + positionDec;
}
